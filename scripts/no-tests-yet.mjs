const target = process.argv[2] ?? 'workspace';

console.log(`${target}: no automated tests are configured yet.`);
console.log('This placeholder keeps CI wired; real critical-flow tests remain a production readiness blocker.');
